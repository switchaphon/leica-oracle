# Built page: regenerated by refresh.sh every 15 minutes and carries a baked-in
# snapshot of CC BY-NC third-party data. The template and build.py are the
# source; rebuild with `python3 build.py ~/hii-data/snapshot.json`.
waterviz/waterviz.html

# Artifact body: same content as CASE-STUDY.html without the doctype wrapper,
# regenerated by build-case-study.py on every run.
.case-study.artifact.html
