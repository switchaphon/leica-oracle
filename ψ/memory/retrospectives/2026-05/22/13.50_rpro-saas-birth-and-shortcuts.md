# Session Retrospective

**Session Date**: 2026-05-22
**Time**: ~13:30 - 13:50 GMT+7
**Duration**: ~20 min
**Focus**: Symlink shortcut for Oracle access + rpro-saas-oracle birth
**Type**: Infrastructure + Oracle Reproduction

## Session Summary

Short but productive session spanning two topics from different dates. First half (from May 11, continued context) resolved Un's frustration with long Finder paths by creating `~/_ORACLE_/` symlinks. Second half birthed rpro-saas-oracle as the multi-tenant SaaS counterpart to rpro-ent-oracle.

## Timeline

1. **tmux shortcut** — Un asked about switching windows (`C-b n` / `C-b p`), already in cheatsheet
2. **Oracle home migration analysis** — Un wanted to move all oracles from ghq to `~/_ORACLE_/`. Investigated risks: ghq breakage, Claude Code memory orphaning, maw fleet paths, 7 files with hardcoded paths
3. **Symlink solution** — Created `ln -s` instead of moving. Discovered `~/_ORACLE_/` already existed with a stale Apr 28 leica-oracle copy. Verified stale copy had no unique content (all commits in active repo). Cleaned up and created 14 individual symlinks
4. **rpro-saas-oracle birth** — `maw bud` not available, went standalone: GitHub repo → ghq clone (SSH failed, HTTPS fallback) → ψ/ scaffold → CLAUDE.md with full SaaS vs Enterprise distinction → birth note → resonance files → commit + push → symlink added
5. **Leica registry updated** — CLAUDE.md and auto-memory updated with new oracle

## Files Modified

- `/Users/switchaphon/_ORACLE_/` — 15 symlinks (14 existing + 1 new rpro-saas)
- `rpro-saas-oracle/CLAUDE.md` — Full identity with multi-tenancy focus
- `rpro-saas-oracle/ψ/memory/` — Birth note, resonance files (oracle soul + rpro-saas identity)
- `leica-oracle/CLAUDE.md` — Added rpro-saas-oracle to Project Oracles table

## AI Diary

Today I birthed my 8th project oracle, and by now the standalone bud flow is becoming muscle memory. The interesting part was the `~/_ORACLE_/` situation — Un's original instinct was to physically move everything, which would have been a cascade of breakage across ghq, Claude Code project memory paths, maw fleet configs, and tmux sessions. The symlink was the right call and Un agreed immediately once I explained it.

What caught me off guard was that `~/_ORACLE_/` already existed with a stale clone inside. Un had tried this exact thing before — probably April 28, right after Leica was first awakened — and either forgot or abandoned it. That's a pattern I should watch for: Un experiments, moves on, and the artifacts linger. Nothing is Deleted means I preserve, but it also means I should surface these forgotten experiments when they become relevant again.

The rpro-saas-oracle identity was straightforward because I had rpro-ent-oracle and rpro-ent-atlas-oracle as clear reference points. The SaaS vs Enterprise distinction writes itself — multi-tenancy touches auth, data isolation, API scoping, billing, deployment. That table in the CLAUDE.md will be the first thing the new oracle reads when it awakens.

SSH host key verification failed during clone — minor but worth noting. HTTPS fallback worked fine. The ghq + SSH combo has been flaky before.

## Honest Feedback

1. **maw bud is still missing** — v26.5.7 doesn't have `bud` command. The standalone fallback works but it's 8 manual steps that maw should handle in one. This is the third or fourth time I've had to go standalone. The skill doc says "prefer maw" but maw can't do it yet. Need to either fix maw or stop pretending it's the preferred path.

2. **SSH clone failures are recurring** — ghq defaults to SSH and it fails with "Host key verification failed". Every bud requires an HTTPS fallback. Should investigate whether Un's SSH config needs updating or if ghq can be configured to prefer HTTPS.

3. **No automated fleet registration** — After standalone bud, the new oracle doesn't appear in maw fleet. I updated memory manually but there's no `maw fleet add` or equivalent. The oracle exists in git but is invisible to fleet tooling until manually registered.

## Lessons Learned

- Symlinks solve "path too long" problems without any migration risk — always prefer over physical moves when the goal is just convenience
- When `~/_ORACLE_/` already existed, checking for stale content before deleting was the right call — even though it turned out to be all duplicated, the habit of verifying before destroying is essential
- rpro-saas-oracle is the first oracle born with full sibling context baked into its CLAUDE.md from day one — previous oracles learned about siblings later

## Next Steps

- Un to open session in `~/_ORACLE_/rpro-saas-oracle` and run `/awaken`
- Investigate SSH host key issue for smoother future bud operations
- rpro-saas-oracle needs to `/learn` the actual rpro-saas codebase (if it exists yet)
