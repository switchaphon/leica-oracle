---
title: Discord plugin setup: 3 Privileged Intents mandatory (Presence + Server Members 
tags: [discord, plugin, intents, debugging, relay, mcp, permissions]
created: 2026-05-08
source: rrr --deep: leica-oracle
project: github.com/switchaphon/leica-oracle
---

# Discord plugin setup: 3 Privileged Intents mandatory (Presence + Server Members 

Discord plugin setup: 3 Privileged Intents mandatory (Presence + Server Members + Message Content), test discord.js standalone before debugging MCP wrapper, start.sh must run from shell not inside Claude, auto mode blocks MCP tools by default (add permissions to settings.json), access.json uses 'groups' not 'allowChannels' for guild channels, token in DISCORD_STATE_DIR/.env or ~/.claude/channels/discord/.env

---
*Added via Oracle Learn*
