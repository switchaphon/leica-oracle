---
title: Cross-session file ownership: when running multiple Claude sessions on overlappi
tags: [multi-session, file-ownership, shared-components, cross-session, design-system]
created: 2026-04-29
source: rrr --deep: pops-clinic-oracle — pickup-queue-to-opd session
project: github.com/switchaphon/pops-clinic-oracle
---

# Cross-session file ownership: when running multiple Claude sessions on overlappi

Cross-session file ownership: when running multiple Claude sessions on overlapping code, assign files to sessions explicitly. Extract shared components to shared/ as the real fix — relay messages align decisions but not file state. Decisions travel well across sessions; exact class strings don't. The pattern: each session owns files, shared code breaks conflicts.

---
*Added via Oracle Learn*
